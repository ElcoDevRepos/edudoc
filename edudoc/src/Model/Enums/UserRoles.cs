using System.Data.SqlClient;
using System.Data.SqlClient;
namespace Model.Enums
{
    public enum UserRoles
    {
        Admin = 1,
        AccountManager = 2,
        AccountAssistant = 3,
        SchoolDistrictAdmin = 4,
        Provider = 5,
    }
}
