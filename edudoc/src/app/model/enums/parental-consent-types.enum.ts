export enum ParentalConsentTypesEnum {
    ConfirmConsent = 1,
    NonConsent = 2,
    PendingConsent = 3,
}

export enum ParentalConsentOptionsEnum {
    ConfirmConsent = 1,
    NonConsent = 2,
    PendingConsent = 3,
    PendingConsentWithEncounters = 4,
    NonConsentWithEncounters = 5,
}
